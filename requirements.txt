# Core Data & Math
numpy==1.26.0          # Pinned for stability (widely used in sklearn/pandas)
pandas==2.1.1          # Latest stable (supports Python 3.8+)
scikit-learn==1.3.2    # Matches your model's version (critical for reproducibility)

# Visualization
matplotlib==3.8.0      # Compatible with seaborn 0.13
seaborn==0.13.0        # Modern version with bug fixes
plotly==5.18.0         # Latest stable (works with Streamlit)

# Streamlit & App Tools
streamlit==1.31.1      # As per your choice (latest stable)
joblib==1.3.2          # For model serialization (matches sklearn)

# Additional Libraries
xgboost==2.0.2         # Latest stable (GPU support if available)
requests==2.31.0       # Secure HTTP requests
openpyxl==3.1.2        # For Excel I/O (pandas dependency)
# For advanced visualizations
plotly-express==0.4.1  # Included in plotly>=5.0 but can be pinned

# For async requests (if needed)
aiohttp==3.8.6         # Alternative to requests

# For progress bars
tqdm==4.66.1           # Useful for data loading
